<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobdeve.s15.group8.mobdeve_mp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <meta-data
        android:name="CLOUDINARY_URL"
        android:value="cloudinary://dnoqllk3h"/>
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:fullBackupContent"
        android:theme="@style/Theme.MOBDEVEMP">
        <activity
            android:name=".controller.activities.ViewAllJournalsActivity"
            android:exported="true" />
        <activity
            android:name=".controller.activities.ViewSinglePlantActivity"
            android:exported="true" />
        <activity android:name=".controller.activities.ViewAllPlantsFragment" />
        <activity android:name=".controller.activities.LoginActivity" />
        <activity android:name=".controller.activities.DashboardActivity" />
        <activity android:name=".controller.activities.AddPlantActivity" />
        <activity android:name=".controller.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>